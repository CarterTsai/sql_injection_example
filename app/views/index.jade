extends layout

block content
  h1= title
  p Welcome to #{title}
  form(name="add-estimation", method="post")
        div.input
            span.label id
            input(type="text", name="id")
        div.actions
            input(type="submit", value="add")
